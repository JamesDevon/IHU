
public class Presets {
    int bass, midrange, tremble, balance, volume ;
    Presets(int bs , int mid, int trem, int bl, int vol){
        bass= bs;
        midrange = mid;
        tremble = trem;
        balance = bl;
        volume = vol;
    }
    
}
